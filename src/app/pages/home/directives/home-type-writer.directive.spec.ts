import { HomeTypeWriterDirective } from './home-type-writer.directive';

describe('HomeTypeWriterDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeTypeWriterDirective();
    expect(directive).toBeTruthy();
  });
});
